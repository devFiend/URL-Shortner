<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <script src="https://cdn.tailwindcss.com"></script> <!-- Tailwind CDN -->
</head>
<body class="flex items-center justify-center min-h-screen bg-gray-100">

    <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md text-center">
        <h2 class="text-2xl font-semibold text-gray-800 mb-4">URL Shortener</h2>

        <form action="/submit" method="post" class="flex flex-col space-y-4">
            <label for="url" class="text-gray-600">Enter URL:</label>
            <input type="text" name="url" id="url" placeholder="Enter a valid URL"
                class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-green-400" required>
            <button type="submit"
                class="bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600 transition duration-300">
                Shorten
            </button>
        </form>

        <div class="mt-4">
            <% if (locals.url) { %>
                <p class="text-gray-700">Your short URL is: 
                    <a href="<%= url %>" target="_blank" class="text-blue-600 underline"><%= url %></a>
                </p>
            <% } else { %>
                <p class="text-gray-500">Enter a URL to shorten.</p>
            <% } %>
        </div>
    </div>

</body>
</html>
